I would like to have a shiny application built with the shiny package in R to demonstrate the querychat R package. It can look similar to the example application in querychat, but please use clinical mock data sets from the random.cdisc.data R package that is available in the current project. For the visualizations you can use the plotly R package and any tables can use the reactable package.